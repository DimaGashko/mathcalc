extends ../_templates/calcPage/calcPage
 
block title
   title #{pagesConfig.hash.matrixAddMatrix.name} | #{pagesConfig.name}
 
block contentTitle 
   | #{pagesConfig.hash.matrixAddMatrix.name}

block fullWidthContent
   - fullWidthContent = true

append workspace

   .controls
      label Operation: 
         select.selectOperator
            option(value="+" selected) Addition
            option(value="-") Subtraction 

   .calc 
      .calc__item.calc__matrixA
      .calc__item.calc__matrixB
      .calc__item.calc__matrixC